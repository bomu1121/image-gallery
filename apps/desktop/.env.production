# 环境
VITE_APP_NODE_ENV = 'production'

# 公共路径
VITE_APP_PUBLIC_PATH = "/portal"

# 接口基础url (服务端与前端部署在同个域下, 所以这里可不设置)
VITE_APP_BASE_URL = ''

# 接口公共前缀
VITE_APP_API_PUBLIC_PREFIX = '/api'

# 获取文件url地址 (服务端与前端部署在同个域下, 所以这里可不设置)
VITE_APP_IMAGE_BASE_URL = ''

# 使用示例
# import.meta.env.VITE_APP_BASE_URL