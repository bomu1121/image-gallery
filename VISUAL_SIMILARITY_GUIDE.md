# 视觉相似性分析功能使用指南

## 功能概述

新的视觉相似性分析功能专门用于识别视觉上相似的图片，特别适合以下场景：

- **人物识别**：识别同一人物的不同照片（如"A 网友"的聊天记录截图）
- **场景识别**：识别相似的场景或环境
- **风格识别**：识别相似的视觉风格或色调

## 使用方法

### 1. 选择分析模式

在图片详情页的标签区域，你会看到一个新的分析模式选择器：

- **视觉相似性分析**：基于视觉特征识别相似图片（推荐用于人物识别）
- **语义分析**：基于语义特征分析图片内容（原有的分析方式）

### 2. 进行视觉分析

1. 选择"视觉相似性分析"模式
2. 点击"视觉分析"按钮
3. 等待 AI 分析完成
4. 查看推荐的标签列表

### 3. 理解分析结果

视觉相似性分析会：

- 提取图片的视觉特征向量（面部特征、发型、服装、背景等）
- 与现有标签库中的图片进行视觉相似度比较
- 推荐相似度超过阈值的标签

## 技术特点

### 视觉特征提取

系统会提取以下维度的视觉特征：

- **面部特征**：眼睛、鼻子、嘴巴、脸型、表情
- **外观特征**：发型、服装、配饰
- **视觉风格**：颜色搭配、构图、光影效果
- **背景环境**：场景类型、物品特征
- **独特标识**：能够区分不同人物或场景的特征

### 相似度计算

采用多维度相似度计算：

- **特征重叠度**（50%权重）：直接匹配的特征数量
- **语义相似度**（30%权重）：语义相关的特征匹配
- **结构相似度**（20%权重）：特征向量的结构相似性

### 智能阈值

- 默认相似度阈值：70%
- 可根据实际效果调整阈值
- 支持动态优化

## 使用场景示例

### 场景 1：A 网友聊天记录

1. 给第一张"A 网友"的聊天记录截图打上"A 网友"标签
2. 对第二张"A 网友"的聊天记录使用视觉相似性分析
3. 系统会识别头像的视觉特征（面部特征、发型等）
4. 推荐"A 网友"标签，因为视觉特征相似

### 场景 2：同一场景的不同角度

1. 给某张室内照片打上"客厅"标签
2. 对另一张客厅照片使用视觉相似性分析
3. 系统会识别背景环境、物品特征
4. 推荐"客厅"标签

## 配置说明

### AI 服务配置

视觉相似性分析支持以下 AI 服务：

- **硅基流动**（推荐）：中文理解能力强，视觉特征提取准确
- **OpenAI GPT-4 Vision**：强大的多模态理解能力
- **智谱 AI**：优秀的国产大模型
- **月之暗面 Kimi**：长文本处理能力强
- **字节豆包**：商业化程度高

### 性能优化

- 特征缓存：避免重复提取相同图片的特征
- 批量处理：支持批量构建标签特征库
- 智能限制：限制单次处理的图片数量，避免 API 调用过多

## 故障排除

### 常见问题

1. **"无法提取图片视觉特征"**

   - 检查 AI 服务配置是否正确
   - 确认网络连接正常
   - 验证图片格式是否支持

2. **"视觉相似度不足"**

   - 调整相似度阈值（默认 70%）
   - 确保标签库中有足够的样本图片
   - 检查图片质量是否清晰

3. **推荐结果不准确**
   - 尝试使用不同的 AI 服务
   - 调整分析模式（视觉相似性 vs 语义分析）
   - 增加标签库中的样本数量

### 调试信息

在浏览器开发者工具的控制台中可以看到详细的调试信息：

- 视觉特征提取过程
- 相似度计算详情
- 推荐结果生成过程

## 最佳实践

1. **标签命名**：使用简洁明确的标签名称
2. **样本数量**：每个标签至少包含 2-3 张代表性图片
3. **图片质量**：使用清晰、特征明显的图片
4. **定期更新**：随着图片库增长，定期重新构建特征库
5. **阈值调整**：根据实际效果调整相似度阈值

## 未来扩展

- 支持更多 AI 服务
- 增加批量分析功能
- 支持自定义特征权重
- 添加用户反馈学习机制
- 支持离线模式分析

## 技术支持

如有问题或建议，请通过以下方式联系：

- 提交 Issue 到项目仓库
- 查看控制台调试信息
- 参考 AI 分析日志
