# 从组图中移除图片功能

## 功能概述

新增了从组图中移除图片的功能，允许用户将组图中的附图移除，使其变为独立图片。

## 功能特点

1. **智能显示**: 只有在查看组图的附图时，才会显示"从组图中移除"按钮
2. **安全确认**: 移除前会弹出确认对话框，防止误操作
3. **自动切换**: 移除当前显示的附图后，会自动切换到主图
4. **实时更新**: 移除后轮播条会实时更新，移除的图片不再显示

## 使用方法

1. 打开图片详情页面
2. 如果当前图片是组图的附图，会在顶部操作栏看到红色的"移除"按钮（减号图标）
3. 点击移除按钮
4. 在确认对话框中点击"确定移除"
5. 图片将从组图中移除，变为独立图片

## 技术实现

### 数据库层面

- 新增 `removeImageFromGroup(imageId)` 函数
- 将图片的 `parentImageId` 设置为 `null`，使其成为独立图片

### 界面层面

- 添加 `isChildImage` 计算属性判断当前图片是否为附图
- 添加移除按钮，只在查看附图时显示
- 实现 `removeFromGroup()` 函数处理移除逻辑

### 用户体验

- 移除按钮使用红色主题，与删除按钮区分
- 提供清晰的确认提示
- 移除后自动切换到主图，避免显示不存在的图片

## 注意事项

1. 只有组图的附图才能被移除，主图无法移除
2. 移除操作不可逆，需要谨慎操作
3. 移除后的图片会保留原有的分组和标签信息
4. 移除操作会触发图片库的刷新事件

## 测试场景

1. **正常移除**: 在组图详情页移除附图
2. **边界情况**: 尝试移除主图（应该不显示移除按钮）
3. **独立图片**: 查看独立图片时不应该显示移除按钮
4. **UI 更新**: 移除后轮播条应该正确更新
5. **自动切换**: 移除当前显示的附图后应该自动切换到主图
