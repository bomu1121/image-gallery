# 组图还原功能

## 功能概述

新增了组图还原功能，允许用户将组图拆分为独立图片。用户可以通过右键菜单将整个组图还原为多张独立图片。

## 功能特点

1. **智能显示**: 只有在右键点击组图的主图时，才会显示"组图还原"选项
2. **安全确认**: 还原前会弹出确认对话框，显示将被还原的图片数量
3. **完整还原**: 将组图的所有图片（主图+附图）都变为独立图片
4. **实时更新**: 还原后图片列表会实时更新，组图关系被完全解除

## 使用方法

1. 在图片库中找到组图（有附图标识的图片）
2. 右键点击组图的主图
3. 在右键菜单中点击"组图还原"
4. 在确认对话框中查看将被还原的图片数量
5. 点击"确定还原"完成操作

## 技术实现

### 数据库层面

- 新增 `restoreGroupToIndividualImages(parentImageId)` 函数
- 查找所有附图，将它们的 `parentImageId` 设置为 `null`
- 返回被还原的图片数量

### 界面层面

- 添加 `isGroupImage(img)` 函数判断是否为组图主图
- 在右键菜单中添加"组图还原"选项
- 实现 `restoreGroupFromContext(img)` 函数处理还原逻辑

### 用户体验

- 使用自定义确认对话框，提供清晰的操作提示
- 显示具体的图片数量信息
- 还原后提供成功反馈
- 自动刷新图片列表

## 确认对话框

确认对话框包含以下信息：

- 操作标题："组图还原确认"
- 详细说明：显示组图名称和将被还原的图片数量
- 操作提示：说明还原后的效果
- 操作按钮：取消/确定还原

## 注意事项

1. 只有组图的主图才能进行还原操作
2. 还原操作不可逆，需要谨慎操作
3. 还原后的图片会保留原有的分组和标签信息
4. 还原操作会触发图片库的刷新事件
5. 如果图片没有附图，会提示无法还原

## 测试场景

1. **正常还原**: 右键点击组图主图进行还原
2. **边界情况**: 尝试还原独立图片（应该不显示还原选项）
3. **附图操作**: 右键点击附图时不应该显示还原选项
4. **UI 更新**: 还原后图片列表应该正确更新
5. **数量统计**: 确认对话框应该显示正确的图片数量
6. **错误处理**: 处理各种异常情况

## 与现有功能的区别

- **单个移除**: 在图片详情页可以移除单个附图
- **组图还原**: 在主页面可以还原整个组图
- **批量操作**: 组图还原是一次性处理整个组图的所有图片
- **操作位置**: 单个移除在详情页，组图还原在主页面
