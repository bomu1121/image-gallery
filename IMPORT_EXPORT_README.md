# 图片资源导入导出功能说明

## 功能概述

本功能为图片库应用添加了完整的数据导入导出能力，采用统一的 ZIP 格式，包含图片文件和元数据的完整备份与恢复。

## 功能特性

### 1. 数据导出

#### 完整数据导出

- **功能**：导出完整的图片库数据（图片文件 + 元数据）
- **格式**：ZIP 压缩包
- **包含内容**：
  - `metadata.json` - 元数据文件（图片信息、分组信息、设置）
  - `images/` - 图片文件夹（按分组组织）
- **结构示例**：
  ```
  image-gallery-backup-2024-01-01.zip
  ├── metadata.json
  └── images/
      ├── 未分组/
      │   ├── image1.jpg
      │   └── image2.png
      └── 我的分组/
          └── image3.gif
  ```
- **用途**：完整备份图片库，便于数据迁移和恢复

### 2. 数据导入

#### 完整数据导入

- **支持格式**：ZIP 压缩包（必须包含 metadata.json 和 images 文件夹）
- **导入模式**：
  - **合并模式**：保留现有数据，添加新数据
  - **替换模式**：清空现有数据，完全替换
- **验证机制**：自动验证 ZIP 文件结构和数据完整性
- **智能恢复**：自动恢复分组结构和图片分组关系

## 使用方法

### 访问导入导出功能

1. 打开应用设置页面
2. 在左侧菜单中点击"导入导出"
3. 进入导入导出设置界面

### 导出操作

#### 导出完整数据

1. 点击"导出完整数据"按钮
2. 系统自动打包所有图片和元数据为 ZIP 文件
3. 文件名格式：`image-gallery-backup-YYYY-MM-DD.zip`
4. 下载完成后即可用于备份或迁移

### 导入操作

#### 导入完整数据

1. 点击"选择备份文件"按钮
2. 选择之前导出的 ZIP 备份文件
3. 在导入选项对话框中选择：
   - 导入模式（合并/替换）
   - 确认替换（如果选择替换模式）
4. 确认导入，系统显示详细进度
5. 导入完成后自动刷新图片库

## 注意事项

### 数据安全

- **替换模式**会清空所有现有数据，请谨慎使用
- 建议在重要操作前先进行数据备份
- 导入过程中请勿关闭浏览器或刷新页面

### 文件限制

- 导入的 ZIP 文件必须包含 metadata.json 和 images 文件夹
- 图片文件必须是浏览器支持的图片格式
- 大文件导入可能需要较长时间，请耐心等待

### 兼容性

- 导出的 ZIP 文件包含版本信息，确保导入兼容性
- 建议使用相同版本的应用进行导入导出操作
- ZIP 文件结构必须符合标准格式

## 技术实现

### 核心技术

- **前端框架**：Vue 3 + Element Plus
- **数据存储**：IndexedDB
- **文件处理**：JSZip（ZIP 文件创建和解析）、File API（文件读取）
- **进度显示**：实时进度条和状态提示

### 数据格式

```json
{
  "version": "1.0",
  "exportTime": "2024-01-01T00:00:00.000Z",
  "images": [
    {
      "id": 1,
      "name": "example.jpg",
      "type": "image/jpeg",
      "size": 1024000,
      "groupId": 0,
      "createdAt": 1704067200000
    }
  ],
  "groups": [
    {
      "id": 0,
      "name": "未分组",
      "description": "默认分组",
      "imageCount": 1,
      "createdAt": 1704067200000
    }
  ]
}
```

## 故障排除

### 常见问题

1. **导入失败**

   - 检查 ZIP 文件格式是否正确
   - 确认 ZIP 文件包含 metadata.json 和 images 文件夹
   - 确认文件未损坏
   - 检查浏览器存储空间

2. **导出失败**

   - 检查浏览器下载权限
   - 确认有足够的数据可导出
   - 检查网络连接

3. **进度卡住**

   - 等待更长时间（大文件需要更多时间）
   - 刷新页面重新尝试
   - 检查浏览器控制台错误信息

4. **ZIP 文件结构错误**
   - 确保 ZIP 文件是由本应用导出的
   - 检查 metadata.json 文件是否存在
   - 检查 images 文件夹结构是否正确

### 联系支持

如遇到其他问题，请检查浏览器控制台错误信息或联系开发者。
